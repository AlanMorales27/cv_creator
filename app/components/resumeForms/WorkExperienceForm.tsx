import { ExperienceItem } from "@/lib/schemas/ExperienceItemSchema"

interface WorkExperienceFormProps {
    section: ExperienceItem
}

export default function WorkExperienceForm({ section }: WorkExperienceFormProps) {
    return <>Trabajo</>
}
